``xia2.ssx``: add opt-in live-processing mode ``wait_for_images.enable=True``, which pauses before each batch until that batch's image data has actually been written to disk (for processing during data collection, where the master H5 VDS declares the full planned length but the underlying data files are filled incrementally via SWMR). Configurable ``wait_for_images.method`` (``swmr``/``file_close``), ``timeout`` and ``interval``.
